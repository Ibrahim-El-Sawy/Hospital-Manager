from odoo import models, fields, api ,_

class SearchAppointment(models.Model):
    _name = "search.appointments"



    # name =fields.Many2one('the.appointments', string="Appointment")
    # age = fields.Integer("Age" ,related="name.age")
    # date_time = fields.Datetime("Date Time", related="name.app_date")
